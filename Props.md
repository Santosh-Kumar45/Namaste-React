*** PR0PS  ***

1> -passing argument to a function is just like passing props to a function component

2> -props is a property which acts as argument to the function

3> -cuisine :- cuisine is a description and it is used in inline 
     use in--------foodCard caption

4> -props is an object     